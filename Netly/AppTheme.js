import { DefaultTheme } from 'react-native-paper';

const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '', 
        accent: '',
        background: '',
        text: '',
    },
};

export default theme;